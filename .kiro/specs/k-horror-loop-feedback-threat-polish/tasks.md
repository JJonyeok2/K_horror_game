# Implementation Plan

- [x] Task 1. 적재 화물 재픽업 루프 완성
  - `VanCargoItem`을 `IInteractable`로 확장한다.
  - 적재 화물을 바라보고 `E`를 누르면 `VanCargoHold`에서 제거되고 플레이어 인벤토리에 다시 들어가게 한다.
  - 재픽업 성공 시 `CargoCount`, `TotalCargoValue`, 손 시야 가림 오브젝트가 즉시 갱신되게 한다.
  - 손 슬롯 부족 등 실패 시 화물은 유지하고 HUD 피드백에 실패 이유를 표시한다.
  - EditMode 테스트를 먼저 작성해 실패를 확인한 뒤 구현한다.
  - 전체 EditMode 테스트와 `git diff --check`를 실행하고 커밋/푸시한다.
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6, 5.1, 5.2, 5.3, 5.4_

- [ ] Task 2. 봉고차 단말기 CRT/탑차 패널 UI 고도화
  - `VanTerminalController` 또는 기존 단말기 표시 컴포넌트를 확장해 적재 가치, 화물 개수, 쿼터, 남은 목표 금액을 한 화면에 표시한다.
  - typewriter 효과를 적용하고, 정산 성공/실패 상태별 시각 피드백 트리거를 추가한다.
  - 중앙 하단 프롬프트가 상호작용 대상별 이름과 동작을 표시하도록 라벨을 정리한다.
  - Canvas 기반 EditMode 테스트로 텍스트 갱신과 상태 전이를 검증한다.
  - Unity 수동 테스트로 단말기 글자가 오브젝트 밖으로 튀어나가지 않는지 확인한다.
  - 전체 EditMode 테스트와 `git diff --check`를 실행하고 커밋/푸시한다.
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 5.1, 5.2, 5.3, 5.4_

- [ ] Task 3. 종이문 파괴와 부적 봉인 기믹 구현
  - `PaperDoorInteraction` 컴포넌트를 추가해 `Intact`, `Torn`, `Sealed` 상태를 관리한다.
  - 플레이어 상호작용 또는 몬스터 공격으로 종이문이 찢기는 상태 전이를 구현한다.
  - 부적 부착 시 일정 시간 AI 통과/파괴가 차단되게 한다.
  - `EnemyBrain` 또는 별도 차단 판정 서비스가 봉인된 문을 통과하지 못하도록 연결한다.
  - 본채/사랑채에 테스트 가능한 종이문/부적 대상 샘플을 배치한다.
  - 전체 EditMode 테스트와 `git diff --check`를 실행하고 커밋/푸시한다.
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5, 5.1, 5.2, 5.3, 5.4_

- [ ] Task 4. 한옥 내부 사운드 오클루전과 최고 위협 환경 연출
  - `ThreatAudioOcclusion`을 추가해 플레이어와 AI 사이 벽/문 Raycast 결과에 따라 볼륨과 로우패스 컷오프를 조절한다.
  - 귀신/도깨비 영역 규칙을 유지하면서 영역별 위협 사운드 큐를 분리한다.
  - 신당 최고 가치 아이템 탈취 시 `ThreatAtmosphereCue`가 등불 깜빡임, 안개/노출 변화, 짧은 HUD 피드백을 트리거하게 한다.
  - 오디오 에셋이 없어도 수치와 상태 전이를 검증할 수 있는 EditMode 테스트를 작성한다.
  - 전체 EditMode 테스트와 `git diff --check`를 실행하고 커밋/푸시한다.
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 5.1, 5.2, 5.3, 5.4_
